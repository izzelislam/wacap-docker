import { Session } from './session';
import { EventBus } from '../events/event-bus';
import { WacapConfig } from '../types';
import { IStorageAdapter } from '../types';
/**
 * Manages lifecycle of Session instances and ensures isolation.
 */
export declare class SessionRegistry {
    private baseConfig;
    private storageAdapter;
    private globalBus;
    private sessions;
    constructor(baseConfig: WacapConfig, storageAdapter: IStorageAdapter, globalBus: EventBus);
    get(sessionId: string): Session | undefined;
    has(sessionId: string): boolean;
    listIds(): string[];
    all(): Map<string, Session>;
    create(sessionId: string, override?: Partial<WacapConfig>): Promise<Session>;
    destroy(sessionId: string): Promise<void>;
    /**
     * Logout and destroy a session (removes credentials)
     */
    logout(sessionId: string): Promise<void>;
    shutdownAll(): Promise<void>;
    startByIds(sessionIds: string[], override?: Partial<WacapConfig>): Promise<Session[]>;
    restartAll(): Promise<Session[]>;
}
//# sourceMappingURL=session-registry.d.ts.map